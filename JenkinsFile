pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        bat 'python -m pip install -r requirements-dev.txt'
        bat 'pytest -q --junitxml=test-results.xml'
      }
      post {
        always {
          junit 'test-results.xml'
        }
      }
    }
  }
}
